# Feishu Setup

Configure the Feishu app credentials in the Web console or `/etc/agent-hub/secrets.env`:

- `FEISHU_APP_ID`
- `FEISHU_APP_SECRET`

Supported transports:

- webhook receiver
- websocket receiver

Group chats require mentioning the bot before a task is submitted.

